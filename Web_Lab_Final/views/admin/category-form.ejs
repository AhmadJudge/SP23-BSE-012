<h4 class="text-center my-4">Create New Category</h4>
<p class="text-muted text-center">
  If the action attribute is empty, the form will be submitted to the same URL on which it is rendered.
</p>
<p class="text-muted text-center">By default, the submission method is GET.</p>

<form
  action=""
  class="container mt-4 p-4 bg-light shadow rounded"
  method="post"
>
  <!-- Title Input -->
  <div class="mb-3">
    <label for="title" class="form-label">Category Title</label>
    <input
      id="title"
      name="title"
      type="text"
      class="form-control"
      placeholder="Enter category title"
      value="Men"
      required
    />
  </div>

  <!-- Description Input -->
  <div class="mb-3">
    <label for="description" class="form-label">Category Description</label>
    <textarea
      id="description"
      name="description"
      class="form-control"
      rows="4"
      placeholder="Enter category description"
      required
    >Men desc</textarea>
  </div>

  <!-- Submit Button -->
  <div class="d-grid">
    <button type="submit" class="btn btn-primary">Create Category</button>
  </div>
</form>
